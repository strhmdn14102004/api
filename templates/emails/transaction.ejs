<!DOCTYPE html>
<html>
<head>
    <title>Transaction <%= transaction.status.toUpperCase() %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        
        .header {
            background-color: 
                <% if (transaction.status === 'success') { %>
                    #4CAF50;
                <% } else if (transaction.status === 'failed') { %>
                    #F44336;
                <% } else { %>
                    #FFC107;
                <% } %>;
            color: white;
            padding: 25px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
        }
        .content {
            padding: 25px;
        }
        .transaction-details {
            background: #f9f9f9;
            border-radius: 6px;
            padding: 20px;
            margin: 20px 0;
        }
        .detail-row {
            display: flex;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 1px solid #eee;
        }
        .detail-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        .detail-label {
            font-weight: bold;
            width: 150px;
            color: #555;
        }
        
        .footer {
            margin-top: 25px;
            font-size: 12px;
            color: #777;
            text-align: center;
            padding: 15px;
            border-top: 1px solid #eee;
        }
        .action-button {
            display: inline-block;
            background-color: #bbbbbb;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 4px;
            margin: 15px 0;
            font-weight: bold;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Transaction <%= transaction.status.toUpperCase() %></h1>
        </div>
        
        <div class="content">
            <p>Hallo <strong><%= user.fullName %></strong>,</p>
            
            <p>Detail Transaksi kamu:</p>
            
            <div class="transaction-details">
                <div class="detail-row">
                    <span class="detail-label">ID Transaksi</span>
                    <span><%= transaction._id %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Tipe Transaksi:</span>
                    <span>
                        <% if (transaction.itemType === 'topup') { %>
                            Top up Saldo
                        <% } else if (transaction.itemType === 'transfer') { %>
                            Transfer Saldo
                        <% } else { %>
                            Pembelian sebuah produk
                        <% } %>
                    </span>
                </div>
                
                <% if (transaction.itemName) { %>
                <div class="detail-row">
                    <span class="detail-label">Produk Transaksi:</span>
                    <span><%= transaction.itemName %></span>
                </div>
                <% } %>
                
                <div class="detail-row">
                    <span class="detail-label">Jumlah:</span>
                    <span>Rp<%= transaction.amount.toLocaleString('id-ID') %></span>
                </div>
                
                <div class="detail-row">
  <span class="detail-label">Tanggal Transaksi :</span>
  <span>
    <% 
    const userTimezone = user.timezone || 'Asia/Jakarta';
    const formatOptions = {
      weekday: 'long',
      year: 'numeric',
      month: 'long',
      day: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
      timeZone: userTimezone
    };
    %>
    <%= new Date(transaction.createdAt).toLocaleString('id-ID', formatOptions) %>
    (<%= userTimezone %>)
  </span>
</div>
                
                <div class="detail-row">
                    <span class="detail-label">Status Transaksi:</span>
                    <span class="status"><%= transaction.status %></span>
                </div>
                
                <% if (transaction.metadata && transaction.metadata.recipientName) { %>
                <div class="detail-row">
                    <span class="detail-label">Penerima:</span>
                    <span><%= transaction.metadata.recipientName %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.recipientPhone) { %>
                <div class="detail-row">
                    <span class="detail-label">Nomor Handphone Penerima:</span>
                    <span><%= transaction.metadata.recipientPhone %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.notes) { %>
                <div class="detail-row">
                    <span class="detail-label">Catatan Transaksi:</span>
                    <span><%= transaction.metadata.notes %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.adminNotes) { %>
                <div class="detail-row">
                    <span class="detail-label">Catatan dari admin:</span>
                    <span><%= transaction.metadata.adminNotes %></span>
                </div>
                <% } %>
            </div>
            
            <% if (transaction.status === 'success') { %>
                <p style="color: #4CAF50; font-weight: bold;">✓ Transaksi kamu telah berhasil dilakukan!</p>
                <p>Terima kasih telah menggunakan layanan ini <%= 
                    transaction.itemType === 'topup' ? 'Saldo telah ditambahkan ke akun Anda' : 
                    transaction.itemType === 'transfer' ? 'Transfer telah berhasil dilakukan' : 
                    '' %>.</p>
                
                <% if (transaction.itemType === 'topup' || transaction.itemType === 'transfer') { %>
                <p>Saldo kamu saat ini: <strong>Rp<%= (user.balance || 0).toLocaleString('id-ID') %></strong></p>
                <% } %>
                
            <% } else if (transaction.status === 'failed') { %>
                <p style="color: #F44336; font-weight: bold;">✗ Transaksi kamu gagal dilakukan.</p>
                <p>Tolong coba lagi, atau hubungi admin kami untuk mendapatkan bantuan</p>
                
            <% } else { %>
                <p style="color: #FFC107; font-weight: bold;">⏳ Transaksi kamu sedang diproses.</p>
                <p>Kami akan memberitahu kamu ketika transaksi telah selesai. transaksi ini mungkin membutuhkan waktu dalam beberapa menit.</p>
            <% } %>
            
            <% if (transaction.paymentUrl && transaction.status === 'pending') { %>
                <p>
                    <a href="<%= transaction.paymentUrl %>" class="action-button">Selesaikan pembayaran</a>
                </p>
                <p><small>Link pembayaran akan berhasil dalam 24 jam</small></p>
            <% } %>
            
            <p>Jika kamu tidak melakukan transaksi apapun, abaikan pesan ini. atau hubungi tim support kami untuk mendapatkan bantuan.</p>
        </div>
        
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> SatTech. All rights reserved.</p>
            <p>Pesan ini adalah otomatis, tolong jangan balas pesan ini.</p>
        </div>
    </div>
</body>
</html>