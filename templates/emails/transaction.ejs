<!DOCTYPE html>
<html>
<head>
    <title>Transaction <%= transaction.status.toUpperCase() %></title>
    <style>
        /* ... existing styles ... */
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Transaction <%= transaction.status.toUpperCase() %></h1>
        </div>
        
        <div class="content">
            <p>Hello <strong><%= user.fullName %></strong>,</p>
            
            <p>Your transaction details:</p>
            
            <div class="transaction-details">
                <div class="detail-row">
                    <span class="detail-label">Transaction ID:</span>
                    <span><%= transaction._id %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Customer Name:</span>
                    <span><%= user.fullName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Email:</span>
                    <span><%= user.email %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Phone:</span>
                    <span><%= user.phoneNumber %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Type:</span>
                    <span>
                        <% if (transaction.itemType === 'topup') { %>
                            Top Up Balance
                        <% } else if (transaction.itemType === 'transfer') { %>
                            Balance Transfer
                        <% } else { %>
                            Product Purchase
                        <% } %>
                    </span>
                </div>
                
                <% if (transaction.itemName) { %>
                <div class="detail-row">
                    <span class="detail-label">Product:</span>
                    <span><%= transaction.itemName %></span>
                </div>
                <% } %>
                
                <div class="detail-row">
                    <span class="detail-label">Amount:</span>
                    <span>Rp<%= transaction.amount.toLocaleString('id-ID') %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Date:</span>
                    <span><%= new Date(transaction.createdAt).toLocaleString('id-ID') %></span>
                </div>
                   <div class="detail-row">
                    <span class="detail-label">Status:</span>
                    <span class="status"><%= transaction.status %></span>
                </div>
                
                <% if (transaction.metadata && transaction.metadata.recipientName) { %>
                <div class="detail-row">
                    <span class="detail-label">Recipient:</span>
                    <span><%= transaction.metadata.recipientName %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.recipientPhone) { %>
                <div class="detail-row">
                    <span class="detail-label">Recipient Phone:</span>
                    <span><%= transaction.metadata.recipientPhone %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.notes) { %>
                <div class="detail-row">
                    <span class="detail-label">Notes:</span>
                    <span><%= transaction.metadata.notes %></span>
                </div>
                <% } %>
                
                <% if (transaction.metadata && transaction.metadata.adminNotes) { %>
                <div class="detail-row">
                    <span class="detail-label">Admin Notes:</span>
                    <span><%= transaction.metadata.adminNotes %></span>
                </div>
                <% } %>
            </div>
            
            <% if (transaction.status === 'success') { %>
                <p style="color: #4CAF50; font-weight: bold;">✓ Your transaction was successful!</p>
                <p>Thank you for using our service. The <%= 
                    transaction.itemType === 'topup' ? 'funds have been added to your account' : 
                    transaction.itemType === 'transfer' ? 'transfer has been completed' : 
                    transaction.itemType === 'withdrawal' ? 'withdrawal has been processed' :
                    'product has been processed' %>.</p>
                
                <% if (transaction.itemType === 'topup' || transaction.itemType === 'transfer' || transaction.itemType === 'withdrawal') { %>
                <p>Your current balance: <strong>Rp<%= user.balance.toLocaleString('id-ID') %></strong></p>
                <% } %>
                
            <% } else if (transaction.status === 'failed') { %>
                <p style="color: #F44336; font-weight: bold;">✗ Your transaction failed.</p>
                <p>Please try again or contact our support team if you need assistance.</p>
                
            <% } else if (transaction.status === 'pending') { %>
                <p style="color: #FFC107; font-weight: bold;">⏳ Your transaction is being processed.</p>
                <p>We'll notify you once the transaction is completed. This usually takes a few minutes.</p>
                
                <% if (transaction.paymentUrl) { %>
                    <p>
                        <a href="<%= transaction.paymentUrl %>" class="action-button">Complete Payment</a>
                    </p>
                    <p><small>Payment link will expire in 24 hours</small></p>
                <% } %>
            <% } %>
            
            <p>If you didn't initiate this transaction or need any help, please contact our support team immediately.</p>
        </div>
        
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> Sattech. All rights reserved.</p>
            <p>This is an automated message, please do not reply directly to this email.</p>
        </div>
    </div>
</body>
</html>
                